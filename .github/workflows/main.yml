name: Mirroring

on: [push]

jobs:
  repo-copying:
    runs-on: ubuntu-latest
    steps:
      - uses:  actions/checkout@v2
        with:
          fetch-depth: 0
      - run: ls
      - run: whoami
      - run: pwd
      - run: cp -a . ../temp/copy
      - run: cd ../temp/copy
      - run: rm -fr .git
      - run: ls -a
      